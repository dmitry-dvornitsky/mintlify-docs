---
title: "Requests"
description: "История поисков, импортов и enrichment операций по проекту"
---

<Warning>Deprecated: модуль перепроектирован в V4 — актуальная спека: [Поиск лидов — история запусков](/v4/workspace/strategies/find-leads). Эта страница описывает текущий продукт (V1) и не обновляется.</Warning>

<Warning>Модуль планируется к переработке — текущий UX избыточен. Детали в [THINK.md](/THINK#requests).</Warning>

Requests — журнал всех операций с лидами: импорты, поиски, enrichment. Организован в табы по типу операции.

## Табы

| Таба | Описание |
|---|---|
| **Find Companies** | История поисков компаний |
| **Find Contacts** | История поисков контактов |
| **Find Contacts in Companies** | История поисков контактов внутри компаний |
| **Import Companies** | История импортов компаний |
| **Import Contacts** | История импортов контактов |
| **Company AI Analysis** | История AI-анализов сайтов компаний |
| **LinkedIn Activity** | История LinkedIn активности |
| **Column Enrichment** | История AI-обогащения кастомных колонок |
| **Phone Enrichment** | История обогащения телефонных номеров |

## Phone Enrichment

Запускается из [Leads → Contacts](/features/leads#contacts) через Toolbar. Выбираешь контакты чекбоксами → **Enrich phone numbers**.

Телефоны берутся из Apollo и записываются в профиль контакта. История запусков хранится в табе Phone Enrichment.

## Company AI Analysis

Запускается из [Leads → Companies](/features/leads#companies) через Bulk Action. AI обходит сайты выбранных компаний и проверяет их на соответствие критериям из [Settings → Criteria Groups](/features/settings/criteria-groups).

Таба Company AI Analysis в Requests хранит историю запусков.

## LinkedIn Activity

Запускается из [Leads → Contacts](/features/leads#contacts) через Bulk Action на выбранных контактах.

Система собирает данные со страниц LinkedIn-профилей:
- Активность (посты, комментарии, лайки)
- Полнота профиля
- Наличие фото, опыта, контактных данных и др.

По результатам каждому контакту выставляется **LinkedIn Score** — оценка "живости" аккаунта. Позволяет SDR понять кому писать в LinkedIn, а у кого мёртвый аккаунт без активности.

Таба LinkedIn Activity в Requests хранит историю запусков.

## Текущая структура Import Request

Каждый импорт создаёт **Import Request** — контейнер который может содержать несколько импортов внутри.

Внутри Import Request:

**Статистика:**
- Number of imported companies
- Average company price — стоимость обогащения на компанию (Generect и другие инструменты)

**Табы:**
- **Companies** — все компании из всех импортов в этом реквесте (Company Name, Domain, LinkedIn URL, Industry)
- **Imports** — список отдельных импортов внутри реквеста

**Колонки таблицы Import Companies:**

| Колонка | Описание |
|---|---|
| Request Name | Название реквеста |
| Last Import Status | Статус последнего импорта |
| Companies imported | Количество импортированных компаний |
| Created at | Дата создания |

## Проблема

Текущая концепция: один Import Request = несколько импортов одной тематики (например, все компании с одной конференции). Идея — хранить контекст источника лидов.

На практике это лишний шаг который никому не нужен. Планируется упростить до плоской истории импортов.
